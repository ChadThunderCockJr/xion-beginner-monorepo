/**
 * Centralized image references for the Paywall Passport app.
 * Changing these will update the image everywhere in the app.
 */

export const Images = {
    // Main passport logo used throughout the app
    passportLogo: require('@/assets/images/paywall-passport-icon.png'),

    // App icon (same as passport logo)
    appIcon: require('@/assets/images/paywall-passport-icon.png'),

    // Legacy icons (kept for reference)
    legacyPassportLogo: require('@/assets/images/passport-logo.png'),
    legacyIcon: require('@/assets/images/icon.png'),
};
