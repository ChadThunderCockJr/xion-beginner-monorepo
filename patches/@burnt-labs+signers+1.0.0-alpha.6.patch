diff --git a/node_modules/@burnt-labs/signers/dist/index.js b/node_modules/@burnt-labs/signers/dist/index.js
index 17cf49b..f81b746 100644
--- a/node_modules/@burnt-labs/signers/dist/index.js
+++ b/node_modules/@burnt-labs/signers/dist/index.js
@@ -754,13 +754,13 @@ var AAClient = class extends cosmwasmStargate.SigningCosmWasmClient {
     if (!accountFromSigner) {
       throw new Error("Failed to retrieve account from signer");
     }
-    if (!aaAcount) {
-      throw new Error("Failed to retrieve AA account from chain");
-    }
+    // Handle new accounts that don't exist on-chain yet
+    // For Treasury-sponsored transactions, use default values
+    const effectiveAccount = aaAcount || { accountNumber: 0, sequence: 0 };
     if (explicitSignerData) {
       signerData = explicitSignerData;
     } else {
-      const { accountNumber, sequence } = aaAcount;
+      const { accountNumber, sequence } = effectiveAccount;
       const chainId = await this.getChainId();
       signerData = {
         accountNumber,
@@ -778,14 +778,14 @@ var AAClient = class extends cosmwasmStargate.SigningCosmWasmClient {
         memo
       }
     };
-    const authInfo = makeAAuthInfo(aaAcount, Uint8Array.from(pubKeyBytes), fee);
+    const authInfo = makeAAuthInfo(effectiveAccount, Uint8Array.from(pubKeyBytes), fee);
     const bodyBytes = this.registry.encode(txBodyEncodeObject);
     const authInfoBytes = tx$1.AuthInfo.encode(authInfo).finish();
     const signDoc = tx$1.SignDoc.fromPartial({
       bodyBytes,
       authInfoBytes,
       chainId: signerData.chainId,
-      accountNumber: BigInt(aaAcount.accountNumber)
+      accountNumber: BigInt(effectiveAccount.accountNumber)
     });
     const signature = await this.abstractSigner.signDirect(accountFromSigner.accountAddress, signDoc).then((sig) => {
       return Buffer.from(
diff --git a/node_modules/@burnt-labs/signers/dist/index.mjs b/node_modules/@burnt-labs/signers/dist/index.mjs
index cf559f2..afd9f05 100644
--- a/node_modules/@burnt-labs/signers/dist/index.mjs
+++ b/node_modules/@burnt-labs/signers/dist/index.mjs
@@ -750,13 +750,13 @@ var AAClient = class extends SigningCosmWasmClient {
     if (!accountFromSigner) {
       throw new Error("Failed to retrieve account from signer");
     }
-    if (!aaAcount) {
-      throw new Error("Failed to retrieve AA account from chain");
-    }
+    // Handle new accounts that don't exist on-chain yet
+    // For Treasury-sponsored transactions, use default values
+    const effectiveAccount = aaAcount || { accountNumber: 0, sequence: 0 };
     if (explicitSignerData) {
       signerData = explicitSignerData;
     } else {
-      const { accountNumber, sequence } = aaAcount;
+      const { accountNumber, sequence } = effectiveAccount;
       const chainId = await this.getChainId();
       signerData = {
         accountNumber,
@@ -774,14 +774,14 @@ var AAClient = class extends SigningCosmWasmClient {
         memo
       }
     };
-    const authInfo = makeAAuthInfo(aaAcount, Uint8Array.from(pubKeyBytes), fee);
+    const authInfo = makeAAuthInfo(effectiveAccount, Uint8Array.from(pubKeyBytes), fee);
     const bodyBytes = this.registry.encode(txBodyEncodeObject);
     const authInfoBytes = AuthInfo.encode(authInfo).finish();
     const signDoc = SignDoc.fromPartial({
       bodyBytes,
       authInfoBytes,
       chainId: signerData.chainId,
-      accountNumber: BigInt(aaAcount.accountNumber)
+      accountNumber: BigInt(effectiveAccount.accountNumber)
     });
     const signature = await this.abstractSigner.signDirect(accountFromSigner.accountAddress, signDoc).then((sig) => {
       return Buffer.from(
